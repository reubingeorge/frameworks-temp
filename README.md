# Frameworks Services — homepage preview

Static preview of the new homepage. No build step. Publish directory is the repo root (`.`).

Deploy on Render: New → Static Site → connect this repo → Publish Directory `.` → Create.
